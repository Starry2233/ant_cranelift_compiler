func add(x: i32, y: i32) -> i32 {
    x + y
}

func mul(x: i32, y: i32) -> i32 {
    x * y
}

func main() -> i32 {
	extern "C" func printf(s: str, ...) -> i32;
    let a = 10i32;
    let b = 20i32;
    let c = add(a, b);
	printf("%d\n", c);
	c;
}
