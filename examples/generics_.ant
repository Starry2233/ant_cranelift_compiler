struct A<T> { 
    val: T
}

func f<T>(val: T) -> T {
    val
}

func main() -> i32 {
    let o = new A {
        val = "aaa"
    };

    extern "C" func printf(s: str, ...) -> i32;

    printf("generic struct.val: %s\n", o.val)
    printf("generic func ret val: %s\n", f(o.val))
}